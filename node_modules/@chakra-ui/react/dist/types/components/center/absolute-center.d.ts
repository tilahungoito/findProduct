import { type HTMLChakraProps } from "@chakra-ui/react/styled-system";
export interface AbsoluteCenterProps extends HTMLChakraProps<"div"> {
    axis?: "horizontal" | "vertical" | "both";
}
/**
 * React component used to horizontally and vertically center an element
 * relative to its parent dimensions.
 *
 * It uses the `position: absolute` strategy.
 *
 * @see Docs https://chakra-ui.com/center
 */
export declare const AbsoluteCenter: import("@chakra-ui/react").ChakraComponent<"div", {
    axis?: import("@chakra-ui/react").ConditionalValue<"both" | "horizontal" | "vertical" | undefined>;
}>;
